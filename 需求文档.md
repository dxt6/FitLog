# 健身记录 APP — 需求文档（v1）

> 状态：草案，待确认
> 整理日期：2026-08-10

## 1. 产品概述

一个**安卓端**的健身训练记录工具，核心价值是：按日期清晰记录"练了哪些动作、每组多重、做了几次、左/右手还是双手"，并在某个动作太久没练时主动提醒你。

| 项 | 决定 |
|---|---|
| 平台 | Android（原生 Kotlin + Jetpack Compose） |
| 数据存储 | 手机本地 Room（SQLite），**无账号、无云端** |
| 语言 | 简体中文 UI，重量单位 **kg** |
| 离线 | 完全离线可用，优先本地 |

---

## 2. 核心概念与数据模型

一次使用的心智模型：

```
某一天（训练日）
  └─ 多个动作
       └─ 每个动作多组
            └─ 每组：重量(kg) + 次数 + 单侧/双侧（左 / 右 / 双手）
```

### 实体设计

**动作 Exercise**（动作库中的一条）
- `id`
- `name` 名称（如"哑铃弯举"）
- `muscleGroup` 肌群分类（胸/背/腿/肩/手臂/核心，可选）
- `defaultSide` 默认单双边：`LEFT` / `RIGHT` / `BOTH`（仅作每组默认值，可单独改）
- `isBuiltIn` 是否内置动作（内置不可删，自定义可删）
- `createdAt`

**训练日 WorkoutSession**（某一天的一次训练）
- `id`
- `date` 训练日期（`yyyy-MM-dd`，同一天可有多条或合并为一条）
- `note` 备注（可选，如"练到力竭"）

**当日动作 SessionExercise**（本次训练里练的某个动作，用于排序）
- `id`
- `sessionId`
- `exerciseId`
- `orderIndex` 动作顺序

**组 SetRecord**（核心记录单元）
- `id`
- `sessionExerciseId`
- `setIndex` 第几组
- `weight` 重量(kg，0 或空表示自重/无负重)
- `reps` 次数
- `side` 本次：`LEFT` / `RIGHT` / `BOTH`
- `rpe` 主观疲劳度 RPE（选填，1.0–10.0，可带 .5；不填也可）
- `note` 可选

**间隔提醒规则 ReminderRule**（每个动作一条）
- `exerciseId`
- `enabled` 该动作提醒开关
- `thresholdHours` 间隔阈值（小时，默认 **72**，可单独覆盖）
- 派生：`lastTrainedAt` = 该动作最近一次训练的时间（查询得到，不单独存）

> 说明：左右手/单双边是**每组**可单独设置的字段；动作上的 `defaultSide` 只是新增组时的预填值，不强制统一。

---

## 3. 功能模块

### 3.1 记录训练（核心）
- 进入"今日训练"，默认日期为今天，可切换任意日期。
- 从动作库添加动作到本次训练。
- 每个动作下可添加**多组**。
- 每组录入：**重量(kg)**、**次数**、**左/右/双手**。
- 每组的重量**可单独设置**，不要求一组动作内统一。
- 支持：增/删/改 组、删除动作、拖拽调整动作顺序（可选）。
- 保存即写入本地库。

### 3.2 动作库管理
- 内置一批常见动作，按肌群分类（胸/背/腿/肩/手臂/核心）。
- 用户可**新增自定义动作**（名称 + 肌群 + 默认单双边）。
- 自定义动作可**编辑 / 删除**；内置动作可隐藏，不可删。
- 支持搜索。

### 3.3 按日期查看 / 编辑历史
- 日历或日期选择，查看某天练了哪些动作、每组数据。
- 点击某天可**查看 / 编辑 / 删除**当天的训练。
- 支持"复制上次训练"到今天（提升录入效率，可选增强）。

### 3.4 动作间隔提醒（按动作名）
- 每个动作**独立计时**：取该动作最近一次训练时间。
- 若距上次训练 **> 阈值（默认 72h）** 且提醒开启，发**本地通知**："该练『XX』了"。
- 阈值可**全局默认** + **每个动作单独覆盖**。
- 提醒可**全局总开关** + **单动作开关**。
- 实现：Android `WorkManager` 周期性检查（如每天几次）+ `NotificationCompat` 本地通知；不依赖网络。

### 3.5 统计 / 历史（v1 加强版）
- **历史时间线**：某动作每次训练的重量 / 次数 / RPE / 单双边，按日期排列。
- **PR（个人纪录）**：
  - 最大重量（不限次数）
  - 最大次数（不限重量）
  - 指定次数下的最大重量（如 1RM / 5RM 概念，按次数筛选）
  - 估算 1RM：Epley 公式 `重量 × (1 + 次数 / 30)`
- **训练容量**：某动作 / 某时间段的总容量 `Σ(重量 × 次数)`，支持柱状图。
- **RPE 趋势**：平均 RPE 随时间变化（折线图，基于选填的 RPE）。
- **重量 / 次数趋势**：折线图，可切换指标。
- 图表用 Compose Canvas 自绘，不引第三方图表库。
- 按动作 / 日期范围筛选查看。

### 3.6 设置
- 默认间隔阈值（小时）。
- 提醒总开关。
- 重量单位（默认 kg；预留 lb 开关）。
- 数据导出 / 导入：导出全部数据为 JSON 文件（备份 / 换机），导入 JSON 恢复（导入时可选"合并"或"覆盖"）。

---

## 4. 界面与导航（Compose）

底部导航四个主 Tab：

1. **训练**（今日 / 日历 / 添加训练）
2. **动作库**（内置 + 自定义，搜索）
3. **统计**（动作历史 + 简单图表）
4. **设置**（阈值 / 开关 / 导出）

导航：`Navigation Compose`。主题：Material 3，支持浅色。

---

## 5. 技术架构

| 层 | 选型 |
|---|---|
| UI | Jetpack Compose + Material 3 |
| 架构 | MVVM（ViewModel + StateFlow / Flow） |
| 持久化 | Room（SQLite） |
| 异步 | Kotlin Coroutines + Flow |
| 导航 | Navigation Compose |
| 提醒 | WorkManager（周期任务）+ NotificationCompat |
| 最小 SDK | API 24（Android 7.0）；目标 34+ |

---

## 6. 已确认决策（2026-08-10）

1. **同一天训练**：✅ 一天合并为一条训练记录（不做早/晚分开）。
2. **内置动作**：✅ 内置一批常见动作（按肌群），同时支持自定义新增 / 编辑 / 删除。
3. **统计深度**：✅ 加强版 —— PR（最大重量 / 最大次数 / 指定次数最大重量 / 估算 1RM）、训练容量、RPE 趋势、重量/次数趋势折线图、容量柱状图。RPE 为选填字段。
4. **备份**：✅ 导出 + 导入 JSON 一并做（导入支持"合并"/"覆盖"）。
5. **个人信息 / 计划模板**：❌ v1 不做，先聚焦"记录 + 提醒 + 统计"。

---

## 7. 不在 v1 范围（后续可加）

- 云端同步 / 多设备
- 训练计划模板 / 自动排期
- 社交 / 分享
- 体测数据（围度、体重曲线）
- iOS 版本
