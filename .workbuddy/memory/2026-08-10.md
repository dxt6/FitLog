# 2026-08-10

## 上传 FitLog 到 GitHub
- 新建私有仓库 **dxt6/FitLog**（https://github.com/dxt6/FitLog），用本机 GCM 缓存凭据 + 7897 代理完成。
- 本地已 `git init`，提交 2 个 commit：初始源码（28 文件）+ 补 `gradle-wrapper.jar`。
- 已加 Android 风格 `.gitignore`：排除 `app/build/`、`app/.gradle/`、`local.properties`、构建产物（.dex/.class/.apk/.flat/.len 等）、密钥。
- 补上了缺失的 `gradle/wrapper/gradle-wrapper.jar`（Gradle 8.6，从 gradle/gradle 仓库 v8.6.0 经 Contents API 取得）。
- remote: `https://github.com/dxt6/FitLog.git`（不含 token，由 GCM 提供凭据）。分支 `main`。

## 改为公开 + 发布 Release
- 仓库已从私有改为**公开**（PATCH /repos/dxt6/FitLog private:false）。
- 创建 Release `v1.0.0`（FitLog v1.0.0），并把编译好的 debug APK（`app/build/outputs/apk/debug/app-debug.apk`，15.6MB）作为资产上传，命名 `FitLog-v1.0.0-debug.apk`，状态 `uploaded`。
- 下载地址：https://github.com/dxt6/FitLog/releases/download/v1.0.0/FitLog-v1.0.0-debug.apk
- 注：该 APK 是 debug 构建（debuggable + debug 签名），仅供测试/ADB 安装；正式发布需 release 签名构建。
